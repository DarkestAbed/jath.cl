import { dev } from '$app/environment'

export const title = 'Javier Thompson: un profesional de los datos'
export const description = 'El one-stop-shop para conversar con JATH'
export const url = dev ? 'http://localhost:5173/' : 'https://jath.cl/'
